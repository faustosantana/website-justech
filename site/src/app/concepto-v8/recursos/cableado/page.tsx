import { ResourcePage, resourceMeta } from "@/components/v8/ResourcePage";

export const metadata = resourceMeta("cableado");

export default function Page() {
  return <ResourcePage slug="cableado" />;
}
