"use client";

import { TechScene } from "@/components/TechScene";

export function OpsBoard() {
  return (
    <div className="ops-board">
      <TechScene scene="msp" story={1} caption="Representación de operación — no es una consola que Justech venda como producto." />
    </div>
  );
}
