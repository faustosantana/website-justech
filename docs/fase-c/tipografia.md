# Auditoría tipográfica — v4

- Display: Red Hat Display. Cuerpo: IBM Plex Sans. Datos: IBM Plex Mono.
- Display size: `clamp(2.15rem, 5.2vw, 3.85rem)` — menos líneas en laptop que el 4.6rem v3.
- Eyebrow mínimo 0.8125rem. Scene captions al mismo mínimo.
- Labels de canvas: 13px, no 11px.
- `text-wrap: balance` en titulares; `pretty` en párrafos.
- Titular del hero: máx. ~16ch, dentro de `.copy-safe`.
- Prohibido gráfico absoluto detrás del H1 en familias split. Grid + `.stage-visual`.
- Procesos diferenciadores: `ProcessTrack`, no rieles de palabras.

Revisar en QA: palabras largas (infraestructura, licenciamiento) y zoom 200%.
